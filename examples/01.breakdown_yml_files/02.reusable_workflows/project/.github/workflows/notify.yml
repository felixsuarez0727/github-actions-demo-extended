name: Notify Team

on:
  workflow_call:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Notify team
        run: echo "Changes detected. Notify team via email."
name: Notify Frontend

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/notify-team

